# Used by drizzle-kit (migrate / studio). Not loaded at Worker runtime —
# runtime DATABASE_URL lives in apps/web/.dev.vars and wrangler secrets.
DATABASE_URL="postgresql://user:password@ep-xxx.neon.tech/dbname?sslmode=require"
